[project]
name = "bulletproof-convert-stack"
authors = ["Developer Team"]
description = "A robust smart contract conversion and verification stack"
telemetry = false
requirements = []
[contracts.convert-stack]
path = "contracts/convert-stack.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
